# 房租管理系统 - 抄表功能与账单中心设计方案

## 1. 需求背景

房东微信小程序核心功能之一是抄表和账单管理：
- **抄表功能**：按楼栋房间批量录入水电表数据
- **账单中心**：基于抄表数据自动生成租金和水电费账单，支持房东标记收款状态

## 2. 数据库设计分析

### 2.1 现有数据库表结构
基于 `rental_system_db.sql` 分析，核心表包括：

```
buildings (楼栋表)  
├── rooms (房间表) - 包含费用配置和水电表基数
├── meter_readings (抄表记录表) - 存储每月抄表数据  
└── bills (账单表) - 根据抄表自动生成的账单
```

### 2.2 关键字段说明

**rooms 表关键字段：**
- `monthly_rent`: 月租金
- `water_price`: 水费单价（元/吨）
- `electricity_price`: 电费单价（元/度）  
- `last_water_reading`: 上次水表读数
- `last_electricity_reading`: 上次电表读数

**meter_readings 表字段：**
- `reading_month`: 抄表月份（YYYY-MM）
- `water_reading`: 本期水表读数
- `electricity_reading`: 本期电表读数
- `water_usage`: 计算出的用水量
- `electricity_usage`: 计算出的用电量

**bills 表字段：**
- `bill_month`: 账单月份
- `rent_amount`: 租金
- `water_amount`: 水费
- `electricity_amount`: 电费
- `total_amount`: 总金额
- `status`: 1-待支付 2-已支付 3-已作废

## 3. 功能设计方案

### 3.1 抄表功能设计

#### 3.1.1 页面交互流程
1. **楼栋选择** → **房间列表** → **批量抄表录入**
2. 支持单个房间抄表记录更新
3. 抄表后自动计算用量并更新房间基数

#### 3.1.2 数据处理逻辑
```javascript
// 抄表数据处理流程
1. 获取房间上期读数 (rooms.last_water_reading, last_electricity_reading)
2. 计算本期用量 = 本期读数 - 上期读数  
3. 保存抄表记录到 meter_readings 表
4. 更新房间表的最新读数
5. 触发账单生成逻辑
```

#### 3.1.3 批量抄表优化
- 按楼栋分组显示房间
- 支持拍照记录水电表
- 异常数据校验（负数用量提醒）
- 抄表进度保存（支持分批完成）

### 3.2 账单中心设计

#### 3.2.1 账单生成逻辑
```javascript
// 自动账单生成逻辑
function generateBill(meterReading) {
  const room = getRoomInfo(meterReading.room_id)
  const bill = {
    rent_amount: room.monthly_rent,
    water_amount: meterReading.water_usage * room.water_price,
    electricity_amount: meterReading.electricity_usage * room.electricity_price,
    total_amount: rent_amount + water_amount + electricity_amount
  }
  return bill
}
```

#### 3.2.2 账单状态管理
- **待收款**: 新生成的账单，房东可标记收款
- **已收款**: 房东确认收到款项
- **部分收款**: 支持部分金额收款记录

#### 3.2.3 账单图片生成
- 生成包含费用明细的账单图片
- 支持微信直接发送给租客
- 包含二维码（后续扩展在线支付）

## 4. 技术实现要点

### 4.1 云数据库集合结构
```javascript
// 小程序云开发数据库集合
- buildings: 楼栋信息
- rooms: 房间信息（包含费用配置）
- meter: 抄表记录  
- bills: 账单数据
```

### 4.2 云函数设计
```javascript
// 抄表相关云函数
cloudfunctions/meter/
- action: 'batchRecord' // 批量抄表
- action: 'updateSingle' // 单个房间抄表更新
- action: 'getHistory' // 获取抄表历史

// 账单相关云函数  
cloudfunctions/bill/
- action: 'generate' // 生成账单
- action: 'list' // 获取账单列表
- action: 'markPaid' // 标记收款
- action: 'generateImage' // 生成账单图片
```

### 4.3 数据一致性保证
- 抄表记录与房间基数同步更新
- 账单生成时检查是否已存在同月账单
- 支持抄表记录修正和账单重新计算

## 5. 用户体验优化

### 5.1 抄表页面优化
- 房间按状态筛选（已租/空置）
- 历史数据对比（用量异常提醒）
- 离线数据缓存（网络不好时支持本地保存）

### 5.2 账单管理优化
- 账单按月份/状态分组展示
- 一键标记多个账单已收款
- 收款统计和趋势分析

## 6. 开发优先级

### Phase 1 - 核心功能（当前迭代）
1. ✅ 楼栋房间基础数据结构
2. 🔄 批量抄表功能实现
3. 🔄 基于抄表数据的账单自动生成
4. 🔄 账单列表和状态管理

### Phase 2 - 体验优化
1. 账单图片生成功能
2. 抄表历史对比和异常检测
3. 收款统计和报表功能

### Phase 3 - 高级功能  
1. 租客端账单推送
2. 在线支付集成
3. 自动消息提醒

## 7. 数据流转关系

```
房间创建 → 设置费用标准 → 抄表录入 → 账单生成 → 收款管理
    ↓         ↓           ↓         ↓         ↓
 rooms表   费用配置   meter_readings  bills表  收款记录
```

这个设计方案保证了数据的一致性，同时为房东提供了高效的抄表和收租管理工具。