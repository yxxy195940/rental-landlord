# 房租管理系统 - 房东微信小程序

轻量级房租管理小程序，帮助房东快速管理房产、抄表和收租。

## 当前任务

开发房东端微信小程序

## 技术栈

- 小程序：原生微信小程序
- UI 组件：Vant Weapp
- 状态管理：MobX-miniprogram
- 数据库：微信云开发（云函数+云数据库）
- 消息推送：微信服务号推送

## 需求功能范围

### 1. 房间管理

- 房间列表展示（空置/已租）
- 房间详情查看
- 快速/批量出租登记（确认房间费用，初始化水电表，绑定租客手机号）

### 2. 抄表功能

- 抄表页面

  - 根据当前楼栋房间号，批量抄当月水电表录入

  - 房间抄表记录单点更新

- 抄表后根据房间水电等费用统计生成房间账单

### 3. 账单中心

- 账单列表（待收/已收）
- 账单详情页面，可支持生成图片发送至联系人
- 房东批量标记是否已收款功能

### 4.消息推送

- 当月已生成的账单未支付提醒 房东功能
- 后续迭代功能：开发租客端系统接入服务号后主动推送账单消息功能



## 界面

界面优化建议：该系统的主要功能是房间管理，批量抄表，账单查看。这些模块在首页和底部导航栏的位置放置设计和优化调整



## 页面代码结构

```
pages/
├── index/          # 首页（数据概览）
├── rooms/          # 房间列表
├── roomDetail/     # 房间详情
├── quickRent/      # 快速出租
├── meter/          # 抄表录入
├── bills/          # 账单列表
├── billDetail/     # 账单详情
└── my/             # 我的
```



## 设计规范

- 主色：#1989fa (Vant 白灰蓝)
- 列表采用卡片式设计
- 操作按钮固定底部
- 支持下拉刷新

## 优化实现

1. 整理好代码关系
2. 优化冗余代码
3. 统一UI风格和交互体验



## 问题提示记录：

阅读CLAUDE.md，以及项目代码。

帮助房东快速管理房产、抄表和收租账单的理念。交互方式优化和建议

我的意思是希望用户使用小程序时，快速创建好房间后，是不是要给房间设置初始的水电表的值，如何引导批量快速给新房间设 置当前水电表，并且要在房间详情页面展示，房间详情页面去掉合同开始和合同结束字段。还有就是出租流程，当房间建好后，是不是要有个简单的出租旅程，设置租客手机号姓名等，使用出租登记按钮如何？已出租的房间这个按钮是不是可以变成退租按钮？退租旅程就是清除租客信息？



房间详情页面 添加更新登记信息按钮：

- 可更新登记信息：租客信息，水电信息的当前水表和当前电表
- 不需要显示单独的水电信息设置按钮，在更新登记信息处管理即可
- 出租登记信息的姓名和电话可以非必填，未填写则页面展示未填写

房间管理页面

- 帮我在房间管理页面添加批量出租登记按钮，这个按钮放置在哪里比较合适，代替批量初始抄表的页面功能。点击后可查询出当前楼栋下未出租的房间，即可批量登记租客信息和填写初始水电信息

- 考虑到房间可能比较多的情况，为了批量登记页面便于阅读和填写操作，怎么设计交互比较好

  



### 抄表模块

1. 我希望你修改批量抄表处每个房间展示'上次'的水表和电表的值的逻辑，应该是优先查询上个月该房间meter表的记录，如果查不到，则展示房间对象里的水电表的值。
2. 提交抄表数据接口应该新增或修改meter表的记录，不修改room表的记录。

3. 生成账单的逻辑也一样，应该是优先使用上个月该房间meter表的记录，如果查不到，则使用房间对象里的水电表的值作为上一次水点表的值，用以计算生成水电费。

​	页面优化：帮我调整批量抄表页面代码，样式不对，一个房间的水电表放一排，去掉多余的图标，关键信息加粗醒目即可

为什么抄表页面中 已抄表的房间 左上角状态字段没有更新



疑问：

- 房间的当前水电表字段的必要？如果是为了开租登记填的话，确实可能有这种场景：在月初租掉的房子，需要登记一下水电表数据，此时不好定界是上一月份的数据，所以可以承载上一次查表的含义？
- 在月初和月末抄表的区别？大多数房东应该都是月末抄表，所以进入抄表页面时让用户选中一个抄表月份（默认当前月），业务含义是为代表当月的数据抄表。当用户选中之后代表该月已经开启抄表，后续在进入该页面就默认展示该月抄表情况，如房间已抄表则显示最后更新日期，所以meter表还要有一个月份字段
- 历史抄表记录的查询功能模块，这块的主要目的是记录一下每个房间的抄表历史，抄表时间。我还是做一个下拉框吧，数据是月份，选中后可以即可查询房间的抄表记录，房间的水电表需要显示最后抄表日期
- 抄表页面下方添加按钮“完成抄表”，点击后弹窗是否生成当月账单。生成房间账单，生成当前楼栋的已抄表的账单页面，页面关联月份，日期，费用信息。检查当前项目是否有账单模块代码，有的话基于这些做改动，生成缺少的功能，完善页面和云函数。最终的账单页面支持生成图片转发给微信好友